def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            for k in seq(0, 8):
                x = 0.0
                x = 1.0
            x = 2.0
            x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            for k in seq(0, 8):
                x = 0.0
                x = 1.0
                x = 2.0
            x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            for k in seq(0, 8):
                x = 0.0
                x = 1.0
                x = 2.0
                x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            for k in seq(0, 8):
                x = 0.0
                x = 1.0
                x = 2.0
                x = 3.0
                x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            for k in seq(0, 8):
                x = 0.0
                x = 1.0
                x = 2.0
                x = 3.0
                x = 4.0
                x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            for k in seq(0, 8):
                x = 1.0
                x = 2.0
            x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            for k in seq(0, 8):
                x = 1.0
                x = 2.0
                x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            for k in seq(0, 8):
                x = 1.0
                x = 2.0
                x = 3.0
                x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            for k in seq(0, 8):
                x = 1.0
                x = 2.0
                x = 3.0
                x = 4.0
                x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            for k in seq(0, 8):
                x = 2.0
                x = 3.0
            x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            for k in seq(0, 8):
                x = 2.0
                x = 3.0
                x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            for k in seq(0, 8):
                x = 2.0
                x = 3.0
                x = 4.0
                x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            x = 2.0
            for k in seq(0, 8):
                x = 3.0
                x = 4.0
            x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            x = 2.0
            for k in seq(0, 8):
                x = 3.0
                x = 4.0
                x = 5.0
def bar(n: size, m: size):
    x: f32 @ DRAM
    for i in seq(0, n):
        for j in seq(0, m):
            x = 0.0
            x = 1.0
            x = 2.0
            x = 3.0
            for k in seq(0, 8):
                x = 4.0
                x = 5.0