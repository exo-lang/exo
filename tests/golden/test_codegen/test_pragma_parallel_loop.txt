#include "test.h"

#include <stdio.h>
#include <stdlib.h>

#include <stdio.h>
#include <stdlib.h>

// foo(
//     x : i8[10] @DRAM
// )
void foo( void *exo_restrict ctxt, int8_t*exo_restrict x ) {
#pragma omp parallel for
for (int_fast32_t i = 0; i < 10; i++) {
  int8_t y[10];
  x[i] = y[i];
}
}

